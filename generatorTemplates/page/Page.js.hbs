import React from 'react';
import PropTypes from 'prop-types';
{{#if isCssModules}}
    //styles
    import classes from './{{pascalCase name}}Page.scss';
{{/if}}

const {{pascalCase name}}Page = (props) => {
    return (
        {{#if isCssModules}}
            <div  className={classes.{{camelCase name}}Wrapper}>
        {{else}}
            <div  className="{{camelCase name}}Wrapper">
        {{/if}}
            {{pascalCase name}}Page
        </div>
    );
};

{{pascalCase name}}Page.propTypes = {

};

export default {{pascalCase name}}Page;
